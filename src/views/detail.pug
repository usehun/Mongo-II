extends templates/layout

block content
  h1=movie.title
  p=movie.synopsis
  p year: #{movie.year}
  p rating: #{movie.rating}
  p genre:
  ul
  each genre in movie.genres
    li=genre
  a(href=`${movie.id}/edit`) Edit