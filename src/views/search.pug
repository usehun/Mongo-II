extends templates/layout
include mixins/movies

block content
  form(method="get")
    input(type="text", name="title", placeholder="Title", required)
    input(type="submit", value="Search")
  div
    each movie in movies
      +movie(movie)